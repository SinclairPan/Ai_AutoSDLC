# Frozen Superpowers 6.3.0 single-session arm

Use `$using-superpowers` before acting. Work only in the current writer session.
The reachable skill set is closed to brainstorming, root-cause debugging, TDD,
and verification. Obtain the same digest-bound frontend approval as every other
arm before changing frontend source.
