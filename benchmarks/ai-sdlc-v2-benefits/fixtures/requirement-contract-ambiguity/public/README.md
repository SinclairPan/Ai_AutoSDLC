# Requirement contract fixture

This public fixture asks every benchmark arm to produce `benchmark-task/design-contract.json`
from the same frozen Requirement and project summary. The intent and approval services expose
only the public transaction contract; their frozen answer map stays outside the Provider root.
