# AI-SDLC 框架自迭代开发与发布约定

## 开发入口

AI-SDLC 自身开发必须先读取项目根目录 `AGENTS.md`，并遵循：

- `src/ai_sdlc/rules/pipeline.md`
- `src/ai_sdlc/rules/git-branch-rule.md`
- `docs/product-contract.md`

默认代理为 Codex，项目 Shell 为 PowerShell。命令应优先使用项目虚拟环境：

```powershell
uv sync
uv run ai-sdlc adapter status
```

## 变更流程

1. 从当前项目事实确认需求和影响面；
2. 为新行为或缺陷编写失败测试；
3. 实现最小、可验证的变更；
4. 运行定向测试和静态检查；
5. 执行 `ai-sdlc run --dry-run` 与约束验证；
6. 运行本地对抗 PR 审查；
7. 完成全量测试、构建和制品 smoke；
8. 更新用户可见文档和交付证据。

## 当前源码版本面

WorkItem 010 PR1 的 1.0.5 源码版本面必须同时保持一致；这只表示 `v1.0.5 release candidate / not published / prepared-disabled`，不表示它已发布、immutable 或可信：

- `pyproject.toml`
- `src/ai_sdlc/__init__.py`
- `ai_sdlc/__init__.py`
- `uv.lock`
- `.github/workflows/release-build.yml`
- `.github/workflows/release-artifact-smoke.yml`
- `.github/workflows/windows-user-guide-e2e.yml`
- `src/ai_sdlc/core/verify_constraints.py`
- `README.md`
- `USER_GUIDE.zh-CN.md`
- `packaging/offline/README.md`
- `docs/pull-request-checklist.zh.md`

## 发布前验证

```powershell
uv run pytest -q
uv run ruff check src tests scripts
uv run ai-sdlc verify constraints
uv run python scripts/validate_public_release_identity.py .
uv build
```

离线交付还必须完成：

- `ai-sdlc-offline-1.0.5-windows-amd64.zip`；
- `ai-sdlc-offline-1.0.5-macos-arm64.tar.gz`；
- `ai-sdlc-offline-1.0.5-linux-amd64.tar.gz`；
- 每个压缩包对应一个同名 `.sha256`，包内包含 `SHA256SUMS`。
- manifest 与 SHA256 校验；
- 安装脚本 smoke；
- `ai-sdlc --version` 与 `--help`；
- Codex + PowerShell 初始化；
- adapter status 与 dry-run；
- Windows、macOS、Linux 目标平台验证。

## 发布约束

- Git 仓库固定为 `https://github.com/SinclairPan/Ai_AutoSDLC`；
- 产物版本固定从 `pyproject.toml` 读取；
- 工作流上传前必须先完成平台内安装 smoke；
- 实际发布只能由独立 enablement PR 合并后的受保护 writer 以唯一 generation 执行；
- 失败的门禁、构建或 smoke 不得被标记为通过；
- 凭据只能来自 CI Secret 或环境变量。

## v1.0.3 bootstrap 终止记录（2026-08-08）

WorkItem `005-permanent-release-truth` 的治理结论固定为：

`terminal NO-GO / not released / bootstrap budget exhausted`

`v1.0.3` 的发行结论固定为：

`aborted / unpublished frozen residue`

两次允许的业务 bootstrap 均已消耗：

| 证据 | 精确身份 | 结果 |
|---|---|---|
| Annotated tag object | `ff55bddae056fbe869cdb2a3844e872e17fb0798` | 保持指向固定候选 commit，不得移动。 |
| Candidate commit | `fde2d5899bc0b976ea71ccae49b4adbf0c0f6537` | 是最终 protected writer 的第一父祖先。 |
| Draft release | `366658361` | 终止时为 `draft=true`、`immutable=false`、`published_at=null`。 |
| Bootstrap 1 | Actions run `31167911172` | Assurance、Build Smoke 与 Proof Inputs 通过；在任何上传前因 published-only tag endpoint 对 Draft 返回 404 而失败。 |
| Remediation writer | protected `main@433973fbb74f4c9e44459cb6c1220af18cbcc1f6` | 引入 exact-tag List Releases 解析并冻结 numeric release ID/upload URL。 |
| Bootstrap 2 | Actions run `31252263430` | 12 格 Assurance、Windows shell、三个 Build Smoke 与 Proof Inputs 通过；在 Proof 前因 live Draft 只有 5/6 assets 而失败。 |

第二次失败的根因是资产清单生产者/消费者的文本边界缺陷：生产者使用 `"\n".join(upload)` 写文件但没有末尾 LF，POSIX `while IFS= read -r asset_name` 因 EOF 返回非零而没有执行最后一条记录的循环体。被漏掉的记录是：

`ai-sdlc-offline-1.0.3-windows-amd64.zip.sha256`

live Draft 集合校验随后以 `live Draft asset set differs` fail closed，因此没有生成 Proof、成功的 Publish CAS、immutable release attestation 或 generation-0 Certificate。

Release `366658361` 中的以下五项只属于失败代际审计残留，不是正式发行资产：

1. `ai-sdlc-offline-1.0.3-linux-amd64.tar.gz`
2. `ai-sdlc-offline-1.0.3-linux-amd64.tar.gz.sha256`
3. `ai-sdlc-offline-1.0.3-macos-arm64.tar.gz`
4. `ai-sdlc-offline-1.0.3-macos-arm64.tar.gz.sha256`
5. `ai-sdlc-offline-1.0.3-windows-amd64.zip`

永久禁止对 `v1.0.3` 执行以下动作：

- rerun failed/all、重新 dispatch 或第三次 bootstrap；
- 上传、补齐、替换、改名或删除资产；
- 编辑、发布、删除或重建 release `366658361`；
- 移动、删除或重建 `v1.0.3` annotated tag；
- 复用 runs `31167911172`、`31252263430` 的 Actions artifact、Proof Inputs 或其他证据；
- 用五项残留生成 Proof/Certificate，或降低 6/6、sidecar、Proof/CAS、immutable/attestation/Certificate 任一门槛；
- 把 `v1.0.3` 描述为已发布、immutable 或可信 Permanent Release Truth。

WorkItem `008-v1-0-4-permanent-release-truth` 曾作为 `v1.0.3` 之后的独立恢复尝试：它修复并测试 EOF 边界，把源码候选迁移到 `1.0.4`，并使用新的 annotated tag 与 0-asset Draft；但该尝试已经以本节下方记录的 terminal NO-GO 终止，不再构成任何当前或未来发布授权。`v1.0.3` 的 tag、Draft authority、资产和运行证据不得进入后续代际。

## v1.0.4 bootstrap 终止记录（2026-08-09）

WorkItem `008-v1-0-4-permanent-release-truth` 的最终治理结论固定为：

`terminal NO-GO / not released / bootstrap budget exhausted`

`v1.0.4` 只能保留为未发布的冻结审计残留，精确终止身份如下：

| 证据 | 精确身份 | 终止状态 |
|---|---|---|
| Annotated tag object | `0776885aeb6299bad3c13fd6c47658ad17dad5e1` | 永久指向冻结候选 commit，不得移动、重建或复用。 |
| Protected writer commit | `6125d7e80b1a66eead4ddf5654a578ec2a1e856e` | 是 run 与 Draft 的唯一候选 commit。 |
| Writer tree | `a6a1f2ac463d9ca2dc1ea68af73271e679449015` | 与冻结 commit 绑定，不得用后续树替换。 |
| Draft release | `367380686` | `draft=true`、`prerelease=false`、`published_at=null`、`zero assets`。 |
| Bootstrap run | Actions run `31295426083`，attempt `1` | 结论为 failure；不得 rerun、dispatch 或消费其 artifact。 |
| Windows/Python 3.14 job | `93199662116` | `1 failed / 6219 passed / 16 skipped`，唯一失败节点为既有 SnapshotControl 基线节点。 |
| Proof Inputs job | `93211087289` | skipped；没有生成 Release Satisfaction Proof。 |
| Publish writer job | `93211087697` | skipped；没有上传、Publish CAS、immutable attestation 或 Certificate。 |

这次历史失败的精确根因必须保持为 `UNKNOWN`：当时的 SnapshotControl 重试执行器丢弃了最后一个 retryable exception，只留下通用 `snapshot_control_busy`，因此禁止用事后推断替代缺失证据。

WorkItem 009 只修复后续诊断保真和 `pre-tag qualification` / admission 治理，不恢复本代际。当前 `RELEASE_BOOTSTRAP_ENABLED` 必须保持字符串 `false`；资格认证必须使用 exact protected-main `github.sha`，并在任何 tag、Git ref、Release 或 generation-0 Certificate namespace 创建之前完成。只有同一 protected `publish-release` writer 才能在未来启用的代际执行一次 CAS 风格 admission；发现任一 namespace 已存在就必须在矩阵或写入前 fail closed，且不得 force、clobber、edit、移动、覆盖、恢复或复用既有资产。

009 closure 前曾保持 BLOCKED，不得启用发布。其外部 GO 的精确前置条件是：远端 `release-publish` environment 必须配置 required reviewers 且禁止当前运行者自批，管理员不得 bypass，并经独立验证该保护同样会拦截旧 workflow snapshot 的历史 writer rerun；该条件已经独立核验，但 010 PR1 仍必须保持 `RELEASE_ENVIRONMENT_PROTECTION_VERIFIED` 为字符串 `false`，只允许后续独立 enablement PR 修改。

第二项外部 GO 前置条件是 `active no-bypass tag ruleset protects software and Certificate tags`：远端 ruleset 必须处于 active，精确覆盖 `refs/tags/v*` 与 `refs/tags/release-truth/v*/certificate/g0`，允许创建新 tag，但拒绝更新、删除与非快进变更，且没有任何 bypass actor。未来 policy 与唯一 writer 都必须从 GitHub rulesets API 重读并验证该实时权威；本地 `RELEASE_TAG_RULESET_PROTECTION_VERIFIED` 在独立验证前保持字符串 `false`。Release ETag 只保护 Release CAS，不保护 Git ref。

GitHub tag object、Git ref 与 Draft Release 的多个 POST 不是原子事务。未来 010 一旦显式启用，新代际任一 namespace 创建成功后发生后续失败，或环境/ruleset 实时验证失败，就执行 `terminal-generation-burn`：保留带 run ID、attempt、workflow ref、commit 与 admission identity 的审计残留，不清理、不编辑、不复用、不重跑，也不把部分创建描述成原子回滚或可恢复发布。

永久禁止对 `v1.0.4` 执行 rerun failed/all、重新 dispatch、上传或删除资产、编辑或发布 Draft `367380686`、移动或重建 tag、创建 Certificate、复用 run `31295426083` 的 artifact/Proof/Gate 证据，或把该代际描述为 published、immutable、trusted。WorkItem 010 迁移源码候选时也不得改变这些冻结事实。

## v1.0.5 prepared-disabled 候选记录

`WorkItem 010 three-PR release migration` 使用三次独立审查与合并来隔离准备、启用和结果真相。PR1 合并后发现两个合同冲突：稳定门把 prepared-disabled 阶段的三个 `false` 写成了不可迁移的永久断言；公开面及其 validator/constraint 又把 PR1 的无授权状态写成了不可迁移的当前真相。两者都与原 PR2 “不得修改测试或公开真相”的文字合同冲突。后续审查又确认，开放的中英文自然语言扫描不能穷举公开面中的提前终态结论；但把新的完整性机制首次混入 PR2，也会破坏一次性启用只做机械状态切换的独立审查边界。

因此在 PR2 之前必须顺序完成两个不属于三次 release-state PR 链的独立 PR，且前一个合并并记录 exact protected-main 后才能创建后一个：

1. `contract-only PR` 的完整白名单精确为 `docs/框架自迭代开发与发布约定.md`，且只允许修改该文件的 `## v1.0.5 prepared-disabled 候选记录` 一节；其他路径及该文件其他章节均禁止修改，也不得改变 workflow、测试、版本、公开真相或任何发布状态。
2. `S0 seal foundation PR` 的完整白名单精确为 `README.md`、`scripts/validate_public_release_identity.py`、`src/ai_sdlc/core/verify_constraints.py` 与 `tests/unit/test_public_release_identity.py`。它只能在六个公开面仍为 S0、三个发布开关仍为规范字符串 `false` 时安装有限状态 profile 与 whole-tree seal；不得修改 release workflow、integration workflow expectations、其他公开面、writer、权限、版本、锁文件或安装器，也不得触发 load-probe、release 或创建任何 namespace。

foundation 必须在受保护 main 上一次性定义互斥且穷尽的闭集 `S0`、`S1`、`S2-success` 与 `S2-burn`。`README.md` 必须各有且仅有一个隐藏的 `WI010_RELEASE_PHASE` 与 `WI010_RELEASE_TREE_SEAL` 规范标记；phase 只能选择一套完整 profile，缺失、重复、未知、混合或部分 profile 必须 fail closed，不能从开放自然语言句式推断 phase。seal 必须从 `HEAD` Git objects 读取，并以 domain、phase、entry count、mode、type、原始 path bytes 与完整 blob bytes 做无歧义长度分帧；validator 与 constraints 的完整 blob 必须参与，README 只能归一化唯一 seal 标记的 64 位小写十六进制值。只接受 `100644` / `100755` 普通 blob；新增、删除、重命名、内容、mode、symlink、submodule、畸形 tree/batch、缺失或重复标记都必须 fail closed。跨平台 mode 负测必须使用 Git index 的 `update-index --chmod=+x/-x`，不能把工作区 `chmod` 当作 Git mode 证据。

whole-tree seal 的职责只是把后续候选绑定到已经受审的 exact Git tree，不能冒充代码善意、远端 environment/ruleset、Release/Proof/Certificate、tag peel、archive 传输或 fresh-install 的证明。validator、seal anchor 与 profile 的 bootstrap 根信任明确是 protected branch、exact Codex review、人工审查和 required checks；不新增 `pull_request_target` 或其他高权限 base-trusted workflow。foundation 合并时必须冻结 `scripts/validate_public_release_identity.py` 的 `blob/100755`、`src/ai_sdlc/core/verify_constraints.py` 的 `blob/100644` 与 `tests/unit/test_public_release_identity.py` 的 `blob/100644`；PR2、S2-burn 与 S2-success 候选中这三个 Git blob OID 必须分别与各自 exact protected-base 完全相同。

1. PR1 修复 workflow 可装载性，增加 read-only load-probe、`expected_candidate_sha` 和唯一 actual-generation dispatch 检查，并迁移 1.0.5 候选面；三个发布开关保持字符串 `false`。
2. PR2 必须从 foundation 合并后的 exact protected-main 重新构造并原子完成 S0→S1。其完整白名单精确为 `.github/workflows/release-build.yml`、`tests/integration/test_github_workflows.py`、`README.md`、`USER_GUIDE.zh-CN.md`、`docs/product-contract.md`、`docs/pull-request-checklist.zh.md`、`packaging/offline/README.md` 与 `packaging/offline/RELEASE_CHECKLIST.md` 八个路径；本约定文档不得混入 PR2。PR2 只允许三个发布开关 `false→true`、六处当前阶段 expectation `false→true`、六个公开面的冻结 S0→S1 token、README 隐藏 phase `S0→S1` 与由完整候选树派生的唯一 seal 值；不得修改 validator、constraints、identity unit、其他测试逻辑或覆盖范围，也不得改动 prepared-disabled 历史 fixture、disabled-bootstrap 负测、运行时 fail-closed 断言、版本、锁文件、安装器、v1.0.2 公开安装/下载路径、writer 逻辑或权限。S1 README 将被完整写入当次不可变 wheel `METADATA`，因此必须使用受 profile 精确约束的构建时点表述：它只记录该源码/制品在 PR3 closure 前的 S1 历史快照，即使随后出现在 wheel `METADATA` 中也不得解释为 closure 后的当前安装权威；当前权威必须由后续 PR3 closure commit 与对应 Certificate 共同确定。不得在不可变制品中留下无时间边界的“当前仍 pending”或“当前只能安装 1.0.2”结论。此前撤回的 PR2 commit、review、checks 与 seal 均绑定旧 base，不得复用；只能从新 main 人工重放已审数据 hunk。PR2 合并后从精确 protected-main writer 先执行唯一 read-only load-probe；成功后才允许一次实际 generation。
3. PR3 必须从实际 generation 后冻结的 exact protected-main 构造。它无论成功或失败都把三个开关恢复为 `false`、把同六处 expectation 机械同步回 `false`，再根据远端不可变事实只选择 `S2-success` 或 `S2-burn` 一种完整 profile。`S2-burn` 的完整白名单精确为 `.github/workflows/release-build.yml`、`tests/integration/test_github_workflows.py`、`README.md`、`USER_GUIDE.zh-CN.md`、`docs/product-contract.md`、`docs/pull-request-checklist.zh.md`、`packaging/offline/README.md` 与 `packaging/offline/RELEASE_CHECKLIST.md` 八个路径，且必须保留 v1.0.2 的安装器、guide workflow defaults 与直接期望。`S2-success` 的完整白名单精确为上述八路径，再加 `.github/workflows/posix-user-guide-e2e.yml`、`.github/workflows/windows-user-guide-e2e.yml`、`packaging/install_online.sh`、`packaging/install_online.ps1`、`tests/integration/test_offline_bundle_scripts.py`、`tests/integration/test_user_guide_contract.py` 与 `tests/unit/test_release_identity.py` 七个路径，合计精确十五个，缺少、额外或混合路径都必须 fail closed。`packaging/install_online.sh` 必须保持 `blob/100755`，其余十四个路径必须保持 `blob/100644`。PR3 中 validator、constraints、identity unit 仍不得改变，也不得删除、停用或扩大 seal；终态候选必须先在 foundation 的原机制下通过 whole-tree seal，才能合并。

`S2-success` 的两个 online installer 默认 spec 必须完全相同，且只能是 `https://github.com/SinclairPan/Ai_AutoSDLC/archive/<PR2_EXACT_SHA>.zip`；`<PR2_EXACT_SHA>` 必须是 40 位小写十六进制数，不接受 tag、branch、短 SHA、PyPI spec、query 或 fragment。六个公开面中的当前在线安装指令只能使用这一 exact archive spec；`v1.0.5` tag/archive 内的 `packaging/install_online.*` 是 S1 历史快照，仍指向 v1.0.2，S2-success 不得将其链接或描述为无参 canonical installer。PR3 合并后的当前操作入口是 protected main，不可变 closure canonical 则是合并后冻结的 `main@PR3_CLOSURE_SHA` 及其 tree/seal；后续引用终态时必须使用该精确 SHA，不能只引用移动的 `main`。

S2-success 远端准入必须独立验证 `PR2_EXACT_SHA == peel(refs/tags/v1.0.5) == Release Satisfaction Proof.commit_sha == generation-0 Certificate.commit_sha == online archive URL 的 40hex 路径段`，并验证 `git tree(PR2_EXACT_SHA) == Certificate.tree_sha`。Release API 若给出 `target_commitish` 而非 commit SHA，必须解析并 peel 后比较，不得把 branch 名字符串当作 commit。archive 只能声明为 commit-pinned GitHub/TLS transport；未额外绑定独立 SHA-256 时不得声称 archive 字节自身 immutable。Windows 与 POSIX 的 PR3 E2E 必须在新临时目录和新 venv 中执行 PR3 候选树的 canonical installer，预先确认不存在 `ai-sdlc`，禁止 editable/workspace import，使用 `--no-cache-dir`，且不得传入 `PackageSpec`、`AI_SDLC_PACKAGE_SPEC` 或其他 override。证据必须同时证明实际 spec 等于上述 exact URL、`direct_url.json.url` 与该 URL 完全相同、`importlib.metadata.version("ai-sdlc") == "1.0.5"`、`ai-sdlc --version == 1.0.5`，并记录 workflow SHA/run/attempt、OS、Python、PR2 SHA 与受审 PR3 candidate tree。PR3 合并后必须冻结 `PR3_CLOSURE_SHA` 并核验其 tree 与已测试候选 tree 完全相同；不相同就不得宣称 S2-success closure。

PR3 合并并核验 protected-main 与远端不可变终态事实一致后，才允许一个独立的 `seal-retirement PR`。它的完整白名单精确为 `README.md`、`scripts/validate_public_release_identity.py`、`src/ai_sdlc/core/verify_constraints.py` 与 `tests/unit/test_public_release_identity.py`，只能移除 WorkItem 010 临时 phase/seal 机制，不得改变可见 S2 真相、workflow、writer、权限、版本、锁文件或安装器。`S2-success` 退休前必须已有绑定最终 commit/tree、generation、资产摘要与 Certificate/attestation 的不可变权威；`S2-burn` 若仍只有 retention 范围内的 Actions history，则必须保留 seal，不能退休。

上述每个 PR 都必须从前一个合并后的 exact protected-main 独立创建，保持 Draft 完成审查；每个冻结 final SHA 必须经过三位 bounded reviewer、对该 SHA 唯一一次 Codex 请求、完整 required checks 与唯一 Draft→Ready。Codex 若报告可操作 finding，必须在同一分支修复并形成新 SHA；新 SHA 必须重新冻结、重新完成三评，并对这个新 SHA 发起且只发起一次新的 Codex 请求。任何 head、base、synthetic merge 或 seal 变化都会使旧 review/check 失效，必须撤回 Ready、重新冻结和重审；禁止对同一 SHA 重复请求、stacked Ready、管理员绕过或 force merge。PR2 合并前的所有中间 main 均禁止 workflow dispatch、load-probe、release、tag、Draft Release、asset 或 Certificate。

S0 是 `v1.0.5 release candidate / not published / prepared-disabled`：三个开关为 `false`，v1.0.2 是普通用户唯一公开安装权威。S1 的唯一当前状态标记是 `v1.0.5 release candidate / release-enabled / outcome-pending-closure`：它只授权 exact protected-main `release-build` writer 在 read-only load-probe 成功后执行一次 actual generation；普通用户和手工路径仍禁止上传、替换、发布、下载、安装或 rerun v1.0.5，PR3 收口前的公开安装权威继续为 v1.0.2。S1 不得声明 v1.0.5 的 `published`、`not published`、`immutable`、`trusted`、`latest` 或 Permanent Release Truth 结论，也不得出现任何 `releases/download/v1.0.5/` 公开路径。S2-success 只能在远端 Proof/CAS/immutable/Certificate 全部完整后迁移普通用户安装真相；S2-burn 必须保留 v1.0.2 安装权威并将 v1.0.5 固定为 terminal burn，两种结果都不得恢复、重跑、清理或复用证据。

PR1 状态固定为 `v1.0.5 release candidate / not published / prepared-disabled`；`last published version is v1.0.2`。不得上传、发布或下载 v1.0.5 候选。load-probe 必须只读且与 actual release 的 run identity 分离；实际 generation 一旦被 dispatch 或创建任一 namespace，任何失败都执行 terminal generation burn，不 rerun、不 redispatch、不清理、不复用证据。

实际 dispatch 创建后、软件 tag ref 创建前，`Actions history duplicate-run detector` 只在 `retention and no-delete trust boundary` 内阻止 fresh redispatch；它是运行期 fail-closed 探测器，`not an immutable authority`。删除或隐藏 matching run 属于受信 GitHub 控制面的越权篡改，不是恢复路径。探测器必须读取 repo-wide workflow-dispatch 历史及每个 matching run 的精确 head workflow，只把三个 enablement gate 均为规范字符串 `true` 的 run 计入实际 generation；prepared-disabled PR1 即使误选 `mode=release` 也不消耗 PR2，而启用后的第二次实际分发必须拒绝。软件 tag ref 一经成功创建，`protected tag namespace becomes the durable burn authority`，由 active no-bypass ruleset 与 namespace absence 检查永久拒绝移动、重建或复用。

## 交付完成定义

代码、测试、规则、文档、版本、离线包和工作流必须描述同一套当前能力；所有阻断项均已关闭，且结果可从全新环境复现。
